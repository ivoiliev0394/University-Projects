using System;
using System.Collections.Generic;
using System.Data.SqlClient;
using System.Text;

namespace School
{
    class GetData
    {/*
        //static string connString =
          //  @"Server=localhost\SQLEXPRESS;Datanase=School;Integrated Security = True";
        //static string connString =
        //    @"Server=DESKTOP-JN25LEH;Datanase=School;Integrated Security = True";
        static string queryString =
            "SELECT Name, Patron, City, District, Address, Website, Telephon FROM SchoolData";

        public static void ShowData()
        {
            using(SqlConnection connection=new SqlConnection(connString))
            {
                SqlCommand command = new SqlCommand(queryString, connection);
                connection.Open();
                using(SqlDataReader reader=command.ExecuteReader())
                {
                    while(reader.Read())
                    {
                       
                    }
                }
            }
        }*/
    }
}
